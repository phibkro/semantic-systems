__pycache__/
*.py[cod]
.pytest_cache/
.pyright/
.ruff_cache/
.venv/
dist/
build/
*.egg-info/

# Local stacklit renderings; stacklit.json is the canonical compact index.
DEPENDENCIES.md
stacklit.html

# Disposable, locally materialized research repositories. The checked-in
# source catalog under references/ is canonical; these checkouts are caches.
.references/

# Unreviewed workflow payloads may contain long source excerpts, mixed-status
# claims, or private execution metadata. Check in curated projections only.
.research-cache/

# Bun/JS tooling: installed from the checked-in lockfile, never committed.
node_modules/
bun-debug-*.log
.alchemy/
