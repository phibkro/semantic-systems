# Work contracts

The delegation frontier is advisory. Before assigning a task, create a bounded
contract here containing:

- objective;
- stable inputs and dependencies;
- output artifacts;
- allowed and forbidden scope;
- assumptions;
- acceptance criteria;
- validation commands;
- review owner.
