#!/usr/bin/env bash
set -euo pipefail

root="$(git rev-parse --show-toplevel)"
cd "${root}"

if [[ ! -x ./node_modules/.bin/commitlint ]]; then
  echo "commit-msg: dependencies are missing; run 'bun install --frozen-lockfile'." >&2
  exit 1
fi

./node_modules/.bin/commitlint --edit "$1"
